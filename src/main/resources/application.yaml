spring:
  application:
    name: Krisjson

datasource:
#    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:krisjson}
    url: jdbc:postgresql://localhost:5433/krisjson
    username: postgres
    password: postgres